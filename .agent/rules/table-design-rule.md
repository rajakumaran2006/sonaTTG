---
trigger: always_on
---

The table should include a header section containing a search bar, filter options, a delete option, and an export option.

The export functionality must work based on the currently applied filters — if filters are active, only the filtered data should be exported; if no filters are applied, the entire dataset should be exported.

The table must be scrollable and support both table view and list view. When table view is selected, the data should be displayed in a structured table format. When list view is selected, the data should be displayed in a properly formatted list layout.

The table design should be smooth and visually appealing. The table background should use a dark color theme, with text clearly visible in either white or black to ensure proper readability and contrast.